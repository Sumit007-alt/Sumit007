const course = {
    coursename : "JS in Hindi",
    price : "999",
    instructor : "Chai aur code"
}

console.log(course.instructor);

const {instructor:ins} = course
console.log(ins);   

// From instructure to ins... this is called destructure in object. means to do minization. 

